# 2026-06-04 项目初始化报告

## 本次目标

- 为风电预测项目建立可持续扩展的目录结构。
- 初始化本地版本管理基础设施。
- 固化后续协作规则、服务器交接规则和数据安全规则。
- 尝试创建对应的 GitHub 仓库。

## 本次修改

- 保留并扩展了现有的 `15min-lead/`、`4h-lead/`、`24h-lead/` 目录。
- 新增了公共工程目录：`src/`、`scripts/`、`configs/`、`jobs/`、`tests/`、`docs/reports/worklogs/`、`artifacts/`、`transfer/`、`data/`。
- 新增 `AGENTS.md`，明确工作报告、超算流程、数据位置、代码复用和泄露防护规则。
- 新增 `README.md`、`pyproject.toml` 和若干说明文件，作为项目骨架。
- 新增 `.gitignore`，默认忽略权重、缓存、服务器回传结果和暂存上传数据。

## 为什么这样做

- 将公共代码与按预测时距组织的实验空间分离，便于后续复用和维护。
- 先把本地开发、GitHub 同步、服务器投递和结果回传分析的链路设计清楚，可以减少后续重复调整目录结构的成本。
- 把工作报告和数据安全要求前置到仓库规则里，有助于降低实验混乱和数据泄露风险。

## 验证情况

- 已确认当前工作区原本不是 Git 仓库。
- 已确认当前机器具备 `git` 命令。
- 已确认本地原始数据目录 `C:\Users\caosh\Desktop\WTPC\data` 存在。
- 已尝试通过 GitHub 插件读取账号信息，但当前连接器握手超时，暂未拿到可用远端登录态。
- 已确认本机未安装 `gh`，且当前环境变量中未发现 `GITHUB_TOKEN` 或 `GH_TOKEN`。

## 风险与阻塞

- 当前最大的阻塞是远端 GitHub 仓库创建认证链路不可用。
- 若后续仍无法从本机直接创建 GitHub 仓库，需要用户提供 GitHub 用户名/组织名，或在本机补齐可用登录态后再继续自动创建。

## 下一步建议

- 初始化本地 Git 仓库并检查状态。
- 如能借助现有浏览器登录态，则继续自动创建远端 GitHub 仓库。
- 后续开始具体建模前，先在 `configs/`、`jobs/` 和 `scripts/` 中补齐首批数据准备与训练脚本模板。

