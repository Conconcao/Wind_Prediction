# 本次目标

- 拉取服务器端刚 push 的 `6079` 结果。
- 基于已入仓库的 `test_predictions.csv`，用 `WTPC` 基础特征版多变量功率曲线把风速预测转成功率预测。
- 计算 `RMSE / nRMSE / 合格率`，确认 `6079` 在功率侧的实际表现。

# 实际修改了什么

- 本地执行 `git pull --rebase origin main`，同步远端新提交：
  - `15min-lead/wind/results/remote_runs/xinyang_gwnet_full_derived_ctx_6079/test_predictions.csv`
  - `15min-lead/wind/results/remote_runs/xinyang_gwnet_full_derived_ctx_6079/val_predictions.csv`
  - 以及对应更新后的 `metrics.* / per_turbine / manifest`
- 本地运行：
  - `python 15min-lead/wind/scripts/evaluate_power_from_ws_predictions.py --ws-predictions 15min-lead/wind/results/remote_runs/xinyang_gwnet_full_derived_ctx_6079/test_predictions.csv --output-dir 15min-lead/wind/artifacts/local_debug/power_eval_xinyang_gwnet_full_derived_ctx_6079 --curve-model lgbm`
- 生成功率侧分析产物目录：
  - `15min-lead/wind/artifacts/local_debug/power_eval_xinyang_gwnet_full_derived_ctx_6079/`

# 为什么这样做

- 当前已经不需要重新训练或重新导出，因为 `6079` 的逐样本风速预测已经进仓库，最短路径就是直接在本地做功率映射和评估。
- 继续使用 `WTPC` 的基础特征版 `lgbm` 功率曲线，可以保证这次功率侧结果与前面本地 smoke 和 WTPC 主线口径一致。

# 做了哪些验证

- 仓库同步后确认 `6079` 结果目录已包含：
  - `test_predictions.csv`
  - `val_predictions.csv`
- 功率侧主结果（`6079` test）：
  - `RMSE = 161.168 kW`
  - `nRMSE = 0.06447`
  - `MAE = 109.570 kW`
  - `nMAE = 0.04383`
  - `Bias = -26.891 kW`
  - `合格率 Q = 95.617`
- 对照信息：
  - 风速侧 `6079` test `RMSE = 0.45399 m/s`
  - 同一基础功率曲线在真实风速输入下的 test 上限：
    - `RMSE = 88.544 kW`
    - `nRMSE = 0.03542`
    - `Q = 97.466`
- 说明：
  - 当前功率侧误差明显高于“真实风速输入”的曲线本身上限，主要损失来自风速预测误差，而不是功率曲线映射器本身。

# 当前风险、阻塞和下一步建议

- 当前没有流程阻塞，`6079` 的功率侧结果已经拿到。
- 风险：
  - 这次只评估了 `6079`，还没有对 `6058` 做同口径功率侧对照，因此暂时还不能直接量化“derived_ctx` 相比 `6058` 在功率侧提升了多少。
- 下一步建议：
  1. 用同样流程补评估 `6058` 的 `test_predictions.csv`。
  2. 直接比较 `6058` vs `6079` 的功率侧 `RMSE / nRMSE / Q`，确认风速侧的小幅提升是否能稳定传导到功率侧。
  3. 若需要汇报，可再补一份“风速侧改进 -> 功率侧收益”的简明对照表。

# 涉及的数据、服务器或 GitHub 操作

- GitHub：
  - 本地执行了 `git pull --rebase origin main`，同步服务器端已 push 的 `6079` 预测结果。
- 数据：
  - 读取 `WTPC` prepared 数据：
    - `C:\Users\caosh\Desktop\WTPC\results\exp110_xinyang_split721_direct\prepared`
- 本次未新增代码提交、未再次 push。
