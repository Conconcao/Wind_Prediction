# 2026-06-04 数据目录盘点报告

## 本次目标

- 盘点本地数据目录 `C:\Users\caosh\Desktop\WTPC\data` 的整体结构。
- 识别可直接用于风电预测的数据主表、辅助表和元数据表。
- 初步判断不同站点的数据粒度、规模与建模入口。

## 本次检查内容

- 检查了数据根目录下的顶层站点目录和公共文件。
- 统计了每个顶层目录的文件数量、总体积和主要文件类型。
- 查看了代表性 `csv/json/xlsx` 文件的表头和样例内容。
- 安装并使用 `pyarrow`、`openpyxl` 读取代表性 `parquet/xlsx` 的 schema 与样例。

## 目录结构概况

- 顶层站点目录：
  - `huaian`
  - `huanghai`
  - `linhai`
  - `xinyang`
- 开源数值天气预报目录：
  - `open_nwp`
  - `open_nwp_nbr`
  - `open_nwp_test`
- 公共元数据文件：
  - `风机基本信息汇总.csv`
  - `风机基本信息汇总.xlsx`

## 文件类型与规模

- 主体数据格式是 `parquet`，共 `19` 个文件，总量约 `3978.54 MB`。
- 辅助元数据以 `xlsx/csv` 为主，共 `16` 个文件，总量较小。
- 还有 `3` 个站点分析 `pdf` 文档。

各站点粗略规模：

- `huanghai`: `11` 个文件，约 `2892.88 MB`
- `xinyang`: `7` 个文件，约 `472.53 MB`
- `huaian`: `6` 个文件，约 `417.95 MB`
- `linhai`: `7` 个文件，约 `241.71 MB`
- `open_nwp*`: 总体不到 `4 MB`

## 各类数据理解

### 1. 风机级主表

代表性主表如下：

- `xinyang/ALL_TURBINES_15min_202501-202512_QC2.parquet`
  - `1,423,698` 行，`19` 列
  - 关键字段：`风机`、`时间`、`平均风速`、`平均有功功率`、`平均风向`、`平均机舱方位角` 等
  - 适合做 `15min` 时距的站级或机组级预测建模
- `xinyang/ALL_TURBINES_1min_202501-202512.parquet`
  - `24,177,600` 行，`8` 列
  - 关键字段：`风机`、`时间`、`风速`、`有功功率`、`机舱方位角`、`风向`、`经度`、`纬度`
  - 适合做高频特征提取，再聚合到 `15min/4h/24h`
- `huanghai/GW_ALL_TURBINES_6s_202501-202512.parquet`
  - `196,647,078` 行，`11` 列
  - 关键字段：`风机`、`时间`、`风速`、`有功功率`、`PLC风机状态`、`机舱角度`
  - 是当前最大的数据源，适合后续做精细特征或状态过滤，但本地实验应谨慎切片

### 2. 特殊观测或派生表

- `huaian/ALL_TURBINES_DIRECTION_15min_20231202-20240703_QC1.parquet`
  - 以风向、风向标方向、机舱位置统计量为主
  - 更像专项方向数据表，可作为附加特征源
- `huaian/WRF_pred_interpolated.parquet`
  - `579,560` 行，`16` 列
  - 关键字段：`init_time`、`target_time`、`lead_hour`、`turbine`、`u_140m`、`v_140m`、`ws_140m`、`wd_140m`、`ws_observed`
  - 这是一个已经把 WRF 预报插值到机组位置后的监督学习候选表，价值很高

### 3. 开源 NWP 数据

- `open_nwp/gdex_gfs_xinyang_2025-07-01_2025-12-31_hourly.parquet`
  - `9,200` 行，`22` 列
  - 关键字段：`init_time`、`target_time`、`lead_hour`、`u10`、`v10`、`u100`、`v100`、`ws10`、`wd10`、`ws100`、`wd100`
  - 已经从原始 3 小时间隔插值成逐小时
- `open_nwp/gdex_gfs_xinyang_2025-07-01_2025-12-31_raw.parquet`
  - `3,312` 行，原生 `3h` lead
- `open_nwp_nbr/gdex_gfs_xinyang_nbr1_2025-07-01_2025-12-31_raw.parquet`
  - `3,312` 行，`69` 列
  - 包含 `3x3` 邻域统计量与邻域网格值，适合做空间特征增强

`meta.json` 显示：

- 当前开源 NWP 只针对 `xinyang`
- 站点中心坐标约为 `(120.3102, 33.6611)`
- `n_turbines = 46`
- issue time 为 `00/12` 两个起报时次

### 4. 气象观测 Excel

代表性文件 `pre_QC_气象观测数据.xlsx`：

- 表 `data_preQC` 字段为：
  - `时间`
  - `层高`
  - `温度`
  - `湿度`
  - `风速`
  - `风向`
  - `气压`
- `xinyang` 的清洗后行数约 `169,361`
- `huanghai` 的清洗后行数约 `175,150`

这类表适合：

- 与 SCADA 数据做时间对齐
- 补充站级近地面气象特征
- 作为 NWP 偏差订正的参考

### 5. 风机元数据

`风机基本信息汇总.xlsx` 的 `生成摘要` 给出：

- `xinyang`: `46` 台，厂家 `远景能源`
- `linhai`: `74` 台，厂家 `远景能源, 金风科技`
- `huanghai`: `123` 台，厂家 `联合动力, 远景能源, 金风科技`
- `huaian`: `20` 台，厂家 `远景能源`

按站点 `风机基本信息.csv` 抽样确认：

- `xinyang` 额定容量为 `2.2 MW`
- `linhai` 额定容量为 `2.0/2.1 MW`
- `huanghai` 额定容量为 `1.8/2.0/2.1 MW`
- `huaian` 额定容量为 `2.5 MW`

## 当前理解下的建模入口建议

优先级建议如下：

1. `xinyang`
   - 数据相对完整
   - 同时有 `1min`、`15min`、气象观测、风机元数据、开源 GFS NWP
   - 最适合先打通端到端预测流程
2. `huaian`
   - 有 `1min`、`15min` 方向统计和 `WRF_pred_interpolated`
   - 适合做“机组级 NWP + 观测”路线
3. `huanghai`
   - 数据量最大、机型最复杂
   - 更适合在流程成熟后再做大规模建模
4. `linhai`
   - 结构类似常规站点
   - 可作为后续泛化验证站点

## 风险与注意事项

- 中文字段名较多，后续进入训练代码时建议统一做英文化映射层。
- 不同站点时间粒度不完全一致，`huanghai` 甚至有 `6s` 级数据，需要单独的聚合与质量控制策略。
- `open_nwp` 目前只覆盖 `xinyang`，不能直接泛化到所有站点。
- 当前环境里为数据盘点临时安装了 `pyarrow` 和 `openpyxl`，未修改仓库代码。

## 下一步建议

- 先以 `xinyang` 为首个基线站点，建立：
  - 数据读取器
  - 字段标准化映射
  - `15min / 4h / 24h` 标签生成逻辑
  - 观测与 NWP 的对齐脚本
- 然后再决定是先做：
  - 纯历史功率基线
  - 历史功率 + SCADA 特征
  - 历史功率 + NWP 融合模型

